#include <stdio.h>




int main(int argc, char const *argv[])
{
    int N, M,linha[N][M];
    FILE *f;
    f =fopen(filename, "r");
    if ((N < 2) && (M < 1)){ fprintf(stderr,"Invalid number");}
    return 0;
}
